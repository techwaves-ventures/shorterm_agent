# Copy to .env and fill in real values. .env is gitignored. Never commit secrets.

# --- FurnishedFinder (operator tenant only; other tenants connect in the UI) ---
# The operator's FurnishedFinder login email (OTP login — no password).
FF_USERNAME=

# Name that auto-drafted replies sign off with (the operator host's name).
HOST_NAME=

# Optional webhook for notifications (Slack/Discord/etc). Leave blank to disable.
NOTIFY_WEBHOOK_URL=

# Set to 1 to run headless (no visible window). Default 0 = visible real browser.
HEADLESS=0
# Optional egress proxy for the browser (corporate egress policy, or a fixed IP
# agreed with your own network). Requires BROWSER_PROXY_ACK=1 to take effect.
# Do NOT use a proxy to evade a site's IP block — that is circumvention, not
# automation. If a site blocks you, resolve it with them.
BROWSER_PROXY_ACK=
BROWSER_PROXY_SERVER=
BROWSER_PROXY_USERNAME=
BROWSER_PROXY_PASSWORD=

# Anthropic API key — required for the auto-responder agent (drafting replies).
# Get one at https://console.anthropic.com/. Leave blank to disable drafting.
ANTHROPIC_API_KEY=

# --- Reply channels ----------------------------------------------------------
# Which channels fire when you click Send. Default sends on both.
# Drop "email" to reply on the platform only.
REPLY_CHANNELS=platform,email

# --- Email -------------------------------------------------------------------
# Two providers; Resend is preferred and wins if RESEND_API_KEY is set.
#
# Resend (resend.com): verify your sending domain first, then set both vars.
# RESEND_FROM must be on that verified domain — messages are sent from it and
# Reply-To is set to the host's own address, so guests see the host's name and
# replies reach them while the mail still authenticates (SPF/DKIM). Sending
# directly "as" a host's Gmail is not possible and lands in spam.
RESEND_API_KEY=
RESEND_FROM=Shorterm <hello@yourdomain.com>
#
# SMTP fallback. Gmail: use an App Password for SMTP_PASS (not your account
# password). Note Gmail caps ~500 messages/day and flags bulk automation, so it
# is fine for a pilot but not for production guest email.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=

# --- Auth / multi-tenant -----------------------------------------------------
# Secret used to sign login session cookies. REQUIRED — the dashboard won't
# start without it. Generate one with:  python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
# Operator login (tenant 1 — the original single-user account, owns all existing
# data). On first run the dashboard creates this user from the values below.
# Afterwards, change the password with:  python manage.py set-password <email>
OPERATOR_EMAIL=
OPERATOR_PASSWORD=
# Public signup is closed by default. Set to 1 only when self-serve signup is
# intentionally open; otherwise visitors use the landing-page contact form.
PUBLIC_SIGNUP_ENABLED=
# Set to 1 ONLY for local http development — it turns off the Secure flag on
# session cookies. Never set this on a hosted deployment.
INSECURE_COOKIES=
# Fernet key encrypting tenants' FurnishedFinder email at rest. REQUIRED to let
# tenants connect their FF account (the /connect flow). Generate one with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Losing/changing this key makes stored FF emails undecryptable (tenants reconnect).
FF_CRED_KEY=

# --- Billing (Stripe) --------------------------------------------------------
# Leave STRIPE_SECRET_KEY blank to run billing in safe DEMO mode: "subscribing"
# marks the tenant as Pilot (demo) with no live charge. Set the keys below to
# enable real Checkout + the customer portal + webhooks.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
# Public base URL for Stripe redirect callbacks, e.g. https://app.example.com
PUBLIC_BASE_URL=

# --- Inbound email (lead ingestion without scraping) --------------------------
# Forward FurnishedFinder's notification emails here and leads arrive without any
# automated access to their site. Each tenant gets an unguessable address
# (leads+<tenant>-<token>@INBOUND_EMAIL_DOMAIN) shown in Settings.
# Point your mail provider's inbound webhook at POST /inbound/email and give it
# INBOUND_WEBHOOK_SECRET (sent as the X-Inbound-Secret header). Fails closed.
INBOUND_EMAIL_DOMAIN=
INBOUND_WEBHOOK_SECRET=

# --- Autopilot & digest ------------------------------------------------------
# How often the worker scans for due lifecycle steps (follow-ups, pre-arrival).
AGENT_INTERVAL_SECONDS=300

# --- Storage / deployment ----------------------------------------------------
# Two backends, auto-selected: set DATABASE_URL for hosted Postgres (required on
# serverless like Vercel, where SQLite can't persist), otherwise SQLite is used.
#
# Hosted Postgres (Neon / Vercel Postgres / Supabase). When set, the app ignores
# SQLITE_PATH and stores everything here. Include sslmode=require for most hosts:
#   postgres://USER:PASSWORD@HOST/DBNAME?sslmode=require
DATABASE_URL=
# SQLite DB file location (used only when DATABASE_URL is blank). On a disk-based
# host point this at a mounted persistent disk (e.g. /var/data/leads.db).
SQLITE_PATH=

# Demo tenant credentials created by `python manage.py seed-demo` (optional).
DEMO_EMAIL=demo@shorterm.test
DEMO_PASSWORD=
# Set to 1 to auto-seed the demo tenant on first boot (handy on Vercel, where
# running a one-off CLI is awkward). Idempotent — only seeds if it doesn't exist.
SEED_DEMO_ON_BOOT=

# --- Dashboard bind ----------------------------------------------------------
# Local dev keeps loopback-only. On a hosted service set DASHBOARD_HOST=0.0.0.0
# (the platform terminates TLS in front of the app). $PORT is provided by the host.
DASHBOARD_HOST=127.0.0.1
# Avoid 5000 on macOS — AirPlay Receiver binds ::1:5000 and steals localhost.
DASHBOARD_PORT=5050

# --- Browser facade server (browser_server.py) -------------------------------
# Independent auth-gated HTTP server that fronts the Playwright runner so another
# service can drive FF login/OTP/collect/reply. Fails closed if the bearer token
# or HMAC key is unset. Runs on a browser-capable host; keep loopback-only unless
# a remote caller needs it, and keep the bearer+HMAC requirement at the origin.
# See docs/browser-server.md.
BROWSER_SERVER_HOST=127.0.0.1
BROWSER_SERVER_PORT=6767
BROWSER_SERVER_BEARER_TOKEN=
BROWSER_SERVER_HMAC_KEY=
BROWSER_SERVER_TIMESTAMP_TOLERANCE_SECONDS=300
BROWSER_SERVER_MAX_BODY_BYTES=16384
